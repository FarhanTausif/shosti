export default function AttendeeDashboard({ userName }) {
    return (
        <div>
            <h2>Welcome, Attendee {userName}</h2>
            {/* Add Attendee-specific content here */}
        </div>
    );
}
