export default function MHPDashboard({ userName }) {
    return (
        <div>
            <h2>Welcome, Mental Health Professional {userName}</h2>
            {/* Add MHP-specific content here */}
        </div>
    );
}
